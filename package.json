{
  "name": "maths-au-college",
  "private": true,
  "version": "0.15.0",
  "description": "Application de ressources de mathématiques pour les élèves du collège.",
  "scripts": {
    "vendor": "node scripts/copy-vendor.mjs",
    "build:firebase": "esbuild firebase-source.js --bundle --format=iife --global-name=MathsFirebaseBundle --outfile=firebase-bundle.js --minify",
    "build:site": "node scripts/build-site.mjs",
    "check": "node --check app.js && node --check professeur.js && node --check presentation.js && node --check course-content.js && node --check course-store.js && node --check pdf-export.js && node --check sw.js"
  },
  "dependencies": {
    "firebase": "12.16.0",
    "jspdf": "4.2.1"
  },
  "devDependencies": {
    "esbuild": "0.28.1",
    "firebase-tools": "15.23.0"
  }
}
